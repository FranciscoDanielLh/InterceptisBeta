
import SwiftUI

enum Tab {
    case inicio
    case documentos
    case foro
    case leaf
}

struct ButtonNavBar: View {
    
    
    var body: some View {
        
        Text("Hello World")
    }
    
}

struct ButtonNavBar_Previews: PreviewProvider {
    static var previews: some View {
        ButtonNavBar()
    }
}
